name: processor
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - pandas=2.2.2
  - scikit-learn
  - numpy
  - openpyxl
  - pyyaml
  - sqlalchemy
  - pymysql
  - cyvcf2
  # 1. Necesitas el programa 'pip' como dependencia de Conda:
  - pip
  
  # 2. El listado de paquetes PIP debe ir bajo la clave 'pip:'
  - pip:
    - requests
